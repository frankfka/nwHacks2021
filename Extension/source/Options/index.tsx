import * as React from 'react';
import ReactDOM from 'react-dom';

// import Options from './Options';
import ReaderAppPage from '../ReaderApp/ReaderAppPage';

// ReactDOM.render(<Options />, document.getElementById('options-root'));

// Create sandbox for ReaderApp
ReactDOM.render(<ReaderAppPage />, document.getElementById('options-root'));
